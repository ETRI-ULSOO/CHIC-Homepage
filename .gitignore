node_modules/
dist/
.astro/
.DS_Store
*.log

# Heritage-ULSOO 저장소 파일의 사본 — 그쪽이 단일 원천이므로 복제하지 않는다.
# 참고가 필요하면 gh api로 다시 받는다.
_migration/ulsoo-*
_migration/raw/

# ⚠️ 비공개 데이터 — 절대 커밋 금지.
# 이 저장소는 public이다. 여기서 사이트로 옮기는 자산은 개별 검토 후
# public/ 로 복사한 것만 공개된다. 원본 폴더는 저장소 밖에 머문다.
docs/비공개데이터/
docs/비공개데이터/**

# wrangler 작업 산출물
.wrangler/
