# CHIC 사이트 리뉴얼 — 진행 로그

## 2026-08-03 — 착수 세션 (G2 재개 → G3 프로토타입)

### 목표
채팅 세션의 인수인계 문서([[chic-site-handoff]])를 기준으로 CHIC 홍보 사이트 리뉴얼에 착수.
잠정 가정 A-01~A-04 확정 후 디자인 방향 결정까지 진행.

### 결정사항

| # | 결정 | 비고 |
|---|---|---|
| D-07 | 사이트 성격 = **정보 구조·콘텐츠 보존 + 디자인·형태는 트렌드 리뉴얼** | 초기 "1:1 이관" 해석을 사용자가 정정. 구조만 보존, 표현은 신규 |
| D-08 | 저장소 소유 = **ETRI-ULSOO** (업무 계정) | `gh auth status` 확인 결과 이미 활성 계정 |
| D-09 | Contact = **이메일 표기만**, 폼 없음 (A-01 확정) | 백엔드 계층 0 유지 |
| D-10 | 다이어그램 원본 = **일부만 보유, 확인 필요** (A-02 부분 확정) | 미보유분은 Google Sites 추출본 사용 |
| D-11 | 사이트 형태 = **하이브리드** | Home·Project·Consortium·Contact = 롱스크롤 랜딩 / Results·News = 별도 페이지 |
| D-12 | 디자인 = **CHIC 독자 디자인** (ULSOO 먹과한지는 참고만) | G3 정식 수행 근거 |
| D-13 | 스택·배포 = **Astro + Cloudflare Pages 유지** (D-04·D-05 유지) | ULSOO의 무빌드 vanilla 스택은 채택하지 않음. Astro 7.1.6 확인 (2026-08-03 실측) |
| D-14 | 디자인 = **먹빛 전시실 (Meokbit Gallery)** — 다크 캔버스 + 명조 디스플레이 | G3 4안 중 C 골격 + A 타이포. 근거: 주 자산이 흰 배경 다이어그램이라 어두운 캔버스에서만 초점이 됨 |
| D-15 | 주 악센트 = **Mint `#35e0c4`**, 라이트용 딥 변형 `#0d7a68` | Mint 원색은 라이트 배경 1.52:1로 텍스트 불가 (2026-08-03 실측) |

### 산출물

- [[content-inventory]] — 현행 사이트 14페이지 전수 실측 인벤토리 (`_migration/`)
- `prototypes/g3-directions.html` — G3 디자인 방향 4안 프로토타입 (실제 콘텐츠 적용)
- `_migration/ulsoo-DESIGN.md`, `ulsoo-index.html`, `ulsoo-data.js`, `ulsoo-deploy.yml` — ULSOO 레퍼런스 사본

### 실측으로 밝혀진 사실 (인수인계 문서 정정)

1. **영문판은 9개 중 5개만 존재.** Consortium·News 영문판 부재 → D-02(한·영 완전 대응)와 충돌
2. **`/project`와 `/project/about`는 콘텐츠 완전 동일** (중복 페이지)
3. **Ambition and Objectives는 다이어그램 1장이 전부**, 텍스트 0 (문서는 "정적 텍스트"로 분류)
4. **Results는 논문·특허 목록이 아니라 연차별 연구 결과물 카드 39건** (이미지+제목+설명) → 스키마 변경
5. **ULSOO 사이트는 무빌드 vanilla 스택** (Tailwind/Alpine CDN, 단일 index.html, GitHub Pages) — 인수인계 문서의 D-04·D-05 전제와 다름
6. 다이어그램 판독성 문제 실측 확인 — 원본 1280px 기준 11px 라벨이 본문 칼럼 폭에서 6px급으로 축소됨. **lightbox 확대는 필수 요건**

### 현재 진행도

| 게이트 | 상태 |
|---|---|
| G1 Blind Spot | ✅ 완료 |
| G2 Interview | ✅ 완료 (D-07~D-13) |
| G3 Prototypes | ✅ 완료 — C(다크 랩) 골격 + A(뮤지엄) 타이포 규율로 수렴, [[DESIGN]] 확정 |
| G4 References | ✅ 본보기 코드 제시 — Astro 스캐폴딩 + 수직 관통 1세트, `npm run build` 통과 (2026-08-03 실측) |
| G4.5 Plan | ✅ 완료 — [[PLAN]] |
| G5 Implementation | 🔄 진행 중 — 전 페이지 구현 완료, 배포 연결만 남음 |
| G6 Quiz | ⬜ 구현 확정 후 |

### G4 산출물 — 본보기 코드 (2026-08-03)

Astro 7.1.6 스캐폴딩 + 수직 관통 1세트. `npm run build` 통과, `/ko/` `/en/` 실제 렌더링 확인.

| 파일 | 역할 |
|---|---|
| `astro.config.mjs` | i18n `prefixDefaultLocale: true`, 루트 → `/ko/` 리다이렉트 |
| `src/styles/tokens.css` | [[DESIGN]] 토큰 구현. 값의 단일 원천은 DESIGN.md |
| `src/i18n/ui.ts` · `utils.ts` | UI 라벨 + `getLocale`/`useTranslations`/`alternatePath` |
| `src/layouts/BaseLayout.astro` | head·hreflang·폰트·Header/Footer |
| `src/components/DiagramFigure.astro` | **핵심 컴포넌트** — `<dialog>` 기반 lightbox (Esc·백드롭·이미지 클릭 3경로 닫기 실측 확인) |
| `src/content.config.ts` | D-06 이원화 구현 — news(glob/Markdown) + consortium·home(file/YAML) |
| `src/pages/[locale]/index.astro` | **언어를 라우트 파라미터로 두는 패턴** — 14페이지를 언어마다 복제하지 않는다 |

**구현 중 발견한 사실 (실측)**

1. `getCollection`은 **id 알파벳순**으로 반환한다. YAML 정의 순서가 보존되지 않아 디캐릭이 ETRI(주관기관)보다 앞에 나왔다. → 데이터에 `order` 필드를 두고 명시 정렬. **Results 39건에도 동일하게 적용해야 한다.**
2. dev 서버는 스키마 변경 시 콘텐츠 스토어를 자동 갱신하지 않는다 ("collection does not exist or is empty"). 스키마를 고치면 dev 재시작이 필요하다 — 빌드는 정상이었으므로 오진하기 쉽다.
3. 캡션 접두는 언어별로 달라야 한다 (`圖` / `Fig.`). 하드코딩 시 영문 페이지에 한자가 남는다.

### 다이어그램 규칙 개정 (2026-08-03, 사용자 지적)

**초안 오류:** [[DESIGN]] 초안은 다이어그램을 "흰 배경 도판을 어두운 벽에 건다"로 규정했다. 근거는 *현행 다이어그램이 재색조 불가능한 흰 배경 PNG*라는 전제였는데, **재색조가 가능한 자산(직접 그린 SVG)에까지 그 제약을 적용한 것이 잘못**이다. 결과적으로 '연구개발의 필요성' 다이어그램만 페이지에서 색이 겉돌았다.

**개정:** 다이어그램은 시스템 팔레트 SVG로 재제작하는 것을 원칙으로 한다. 범주는 색이 아니라 위치·라벨로 구분하고 악센트는 Mint 하나만 쓴다. 흰 배경 원본과 재제작 SVG를 **같은 페이지에 섞지 않는다**(조각보 방지).

**실측:** 재제작 시 임의 회색(`#4d5a66`)을 각주·화살표에 썼다가 2.74:1로 기준 미달 확인. 각주는 토큰 `#93a4b3`(7.56:1), 화살표는 `#6b7b8a`(4.45:1, 비텍스트 3:1 충족)로 교정. **SVG 내부에도 토큰 강제 + 명암비 실측이 필요하다.**

### G4.5 산출물

[[PLAN]] 작성 — 바뀔 결정 5건(P-01~P-05) 선두 배치, 구현 7단계, 제외 범위 명시.

### G5 착수 — 랜딩 골격 (2026-08-03)

**확정 (P-01~P-03 응답)**

| # | 결정 |
|---|---|
| D-16 | 다이어그램 **단계적 재제작** — 랜딩 6점 우선, Concept 4점·Results 1점은 원본 대기 |
| D-17 | 영문 콘텐츠 **신규 번역** (P-02). 초안 작성 완료, 연구 용어 최종 표현은 검수 대상 |
| D-18 | Project 관련 3페이지를 랜딩 `#project` 섹션으로 **통합** (P-03) |
| D-19 | **Concept and Approach만 별도 페이지로 분리** — D-16·D-18의 충돌 해소책 (아래) |

**D-19 도출 근거 (파생 결정):** D-18을 그대로 적용하면 Concept의 다이어그램 4점도 랜딩에 올라오는데, D-16에서 그 4점은 원본 이미지로 남는다. 그러면 한 페이지에 재제작 SVG와 흰 배경 원본이 섞여 [[DESIGN]]의 "조각보 금지" 규칙을 위반한다. Concept을 별도 페이지로 두면 랜딩은 재제작 SVG만, Concept은 원본만 갖게 되어 충돌이 사라진다. Concept은 다이어그램이 본문 전체인 가장 무거운 페이지라 독립 페이지가 원래 적절하기도 하다.

**구현 상태**

| 항목 | 상태 |
|---|---|
| 랜딩 5섹션 (과제소개·기대효과·필요성·참여기관·연락처) | ✅ 한/영 렌더 확인 |
| `/{locale}/concept/` 페이지 | ✅ 생성 — 다이어그램 슬롯은 대기 표시 |
| 데이터 파일 | `home` `project` `concept` `consortium` `contact` `news` 6종 |
| 다이어그램 재제작 | **2 / 6** — `necessity.svg` `project-overview.svg` 완료 |
| 빌드 | ✅ 4페이지 (`npm run build`, 2026-08-03 실측) |

**남은 재제작 4점:** Home 본문 · 기대효과 · Ambition and Objectives · 연구개발 추진체계

### 다이어그램 재제작 완료 (2026-08-03)

**D-20 개정:** Concept 4점도 재제작 대상에 포함 (사용자 지시). D-16의 '단계적 재제작'은 사실상 전량 재제작으로 확대되었고, 원본 이미지 대기 항목은 Results 1점만 남는다.

**재제작 결과 — 7점**

| 파일 | 배치 | 내용 근거 |
|---|---|---|
| `project-overview.svg` | 랜딩 #project | /project 연구내용·결과물 원문 |
| `necessity.svg` | 랜딩 필요성 | 현행 필요성 다이어그램 구조 |
| `consortium-structure.svg` | 랜딩 #consortium | 기관명 원문. **기관별 담당 과업은 미명시이므로 표시하지 않음** |
| `concept-process.svg` | Concept 01 | Results 2차년도 5개 대분류 |
| `concept-platform.svg` | Concept 02 | 현행 플랫폼 정의 문장의 5개 항목 원문 |
| `concept-asset.svg` | Concept 03 | Results 애셋 기술 4항목 + 1차년도 애셋 기술 |
| `concept-strategy.svg` | Concept 04 | ⚠️ **재해석 — 검수 필요도 높음** |

**폐기 2점 (제작 후 취소):** `impact.svg` · `objectives.svg`. P-03 랜딩 통합의 결과로 이 두 다이어그램의 내용이 같은 페이지의 기대효과 카드·연구목표 목록과 **완전히 중복**됨을 배치 단계에서 확인. 한 페이지에 같은 문구를 두 번 싣지 않기 위해 삭제. → **랜딩 재제작은 6점이 아니라 3점이 맞다.**

**검수 요청 사항 (사용자 확인 필요)**

1. `concept-strategy.svg` — 현행 사이트에 설명 텍스트가 전무하여, 문서화된 과제 특징 4가지를 '기존 한계 → CHIC의 접근' 대비 구조로 **재구성**했다. 원본의 논지와 다를 수 있다.
2. `concept-process.svg` — 5단계 구분은 Results 대분류에서 도출한 것으로 원본 단계 구분과 다를 수 있다.
3. `concept-asset.svg` — 애셋 정의의 3단 구조(원본→애셋화→활용)는 도출된 것이다.

**구현 상태**

| 항목 | 상태 |
|---|---|
| 다이어그램 재제작 | ✅ 7 / 7 (Results 1점만 원본 대기) |
| Concept 페이지 | ✅ 다이어그램 4점 연결, 대기 슬롯 제거 |
| 랜딩 | ✅ 5섹션 + 다이어그램 3점 |
| 빌드 | ✅ 4페이지, SVG 7점 200 응답·XML 오류 0 (2026-08-03 실측) |

### Results 페이지 구현 (2026-08-03)

**항목 수 정정 — 38건이 아니라 39건이다.** 인벤토리 작성 시 소계를 잘못 합산해(2차년도 ①을 8건으로, 총계를 23건으로 기재) 38이라는 값이 문서·KPI·PLAN에 퍼져 있었다. 실제는 **2차년도 24 + 1차년도 15 = 39건**이며, 관련 기술을 전부 정정했다.

**재발 방지:** 랜딩 KPI의 결과물 수를 하드코딩하지 않고 `results.yaml`에서 산출한다. `home.yaml`에 `AUTO_RESULTS` 표식을 두고 페이지에서 `getCollection('results').length`로 치환한다. 손으로 센 수치는 틀린다.

| 항목 | 내용 |
|---|---|
| `src/data/results.yaml` | 39건 (2차 24 · 1차 15), ko/en 병기 |
| `src/pages/[locale]/results.astro` | 카드 그리드 + 연차·대분류 2축 필터 |
| 분류 | acquisition 10 · asset 8 · analysis 9 · platform 8 · service 4 = 39 |
| 정렬 | 현행 사이트와 동일하게 **2차년도 먼저** (order 101~124), 1차년도 (201~215) |
| 필터 | 클라이언트 측. 조합별 항목 수 실측 검증 (1차+애셋 4 · 2차+서비스 3 · 1차+서비스 1) |
| 이미지 | `image` 선택 필드 — 없으면 텍스트 전용으로 렌더되어 이미지 대기가 페이지를 막지 않음 (P-04) |

### News · 리다이렉트 · 영문 다이어그램 · 폰트 · 배포 준비 (2026-08-03)

**News (5건)** — `src/content/news/{ko,en}/`, 목록 + 본문 페이지. 스키마 변경 2건:
- `date`를 **선택 필드로** 바꿨다. 현행 게시물 5건 중 2건(콜로키움 발표자료, 대전시립미술관)에 날짜 표기가 없다. **없는 날짜를 지어내지 않는다.** 표시할 때는 "날짜 미상"으로 대체한다.
- 날짜가 없으니 날짜 정렬로는 순서가 무너진다. `order` 필드로 현행 사이트의 게시 순서를 따른다.

**구 URL 리다이렉트 14종** — `astro.config.mjs`. 새 도메인에서 옛 경로를 입력해도 404가 나지 않는다. 국문 9 + 영문 5, 산출물 전수 확인.

**영문 다이어그램 7점 (M-10 해소)** — `*-en.svg`. 텍스트가 그림 안에 있으므로 언어별 파일로 존재한다. `diagramSrc()` 유틸이 로케일에 따라 경로를 고른다. **래스터였다면 불가능했을 작업이며, SVG 재제작의 실질적 이득이 여기서 나왔다.**

**폰트 자체호스팅 (M-09 해소)** — Google Fonts CDN 제거. `@fontsource/noto-serif-kr`(한글·라틴 400/600 서브셋) + `@fontsource-variable/playfair-display` + `pretendard`(가변·동적 서브셋). 빌드 산출물에 **외부 요청 0건** 실측 확인, 폰트 100개 파일 5MB(동적 서브셋이라 실제 다운로드는 일부만).

**Cloudflare Pages 배포 준비** — `public/_headers`(보안 헤더 + 해시 자산 영구 캐시), [[README]]에 빌드 설정 문서화. **대시보드 최초 연결은 계정 접근이 필요해 미수행.**

**최종 라우트 33개** (페이지 18 + 리다이렉트 15). `npm run build` 통과 (2026-08-03 실측).

## 2026-08-05 — 비공개 자료 반입과 성과 데이터 확충

### ⚠️ 비공개 데이터 취급 원칙 (D-21)

사용자가 `docs/비공개데이터/`(6GB — 최종보고서·연차 발표평가 PPTX·논문/특허/사업화 PDF 87건)를 작업 폴더에 제공했다. **저장소가 public이므로 폴더 전체를 `.gitignore`로 차단하는 것이 첫 작업이었다** (분석보다 먼저 수행).

- `.gitignore`에 `docs/비공개데이터/` 추가, `git check-ignore`로 실증
- 중간 산출물(추출 텍스트·컨택트시트·렌더 PNG)은 전부 **저장소 밖 스크래치패드**에 생성
- 저장소에 들어가는 것은 **개별 검토를 거쳐 `public/`로 복사한 자산뿐**이다

### 확보한 것 — 성과보고서가 결정적이었다

`05_성과보고서`(5쪽)에 논문·특허·SW·수상의 **공식 목록**이 전부 있었다. 개별 PDF에서 긁어모으는 것보다 정확하고 빠르다. **현행 Google Sites에는 이 목록이 전혀 실려 있지 않았다** — 인수인계 문서가 Results를 "논문·특허·데모"로 예상했다가 실물이 없어 폐기했던 바로 그 데이터다.

| 성과 | 수량 | 처리 |
|---|---|---|
| 학술지 논문 | 3 (+ 보고서 이후 게재 MDPI Heritage 1) = **4** | ✅ 공개 |
| 학술대회 발표 | **21** | ✅ 공개 |
| 특허 출원 | **14** (국내 13 · 미국 1) | ✅ 공개 |
| SW 등록 | **2** | ✅ 공개 |
| 수상 | **1** (문화체육관광부 장관표창) | ✅ 공개 |
| 사업화 23건 · 매출 59.2억 · 고용 13명 | — | ⛔ **보류 — 사용자 판단 필요 (M-16)** |

특허 PDF 중 6건은 텍스트 레이어가 없어(스캔·이미지 렌더) PyMuPDF로 페이지를 PNG로 렌더한 뒤 직접 읽어 서지를 확보했다.

### 사진 17점 반입

최종보고서(128쪽)에서 이미지 268건을 추출 → 중복 제거 207건 → 컨택트시트로 육안 선별 → **17점을 webp로 변환(총 1.4MB)**해 `public/images/{results,photos}/`에 배치.

- Results 카드 **12건**에 이미지 연결 (로봇팔·현장 촬영·3D 스캔·회화 객체검출·지식그래프·가상전시 등)
- 랜딩에 **PhotoPanel** 신설 — 실증 전시 사진 풀블리드 밴드 ([[DESIGN]] 컴포넌트 추가)
- News '2021 공존과 지속 콜로키움'에 키비주얼 연결

### 신규 페이지·데이터

| 항목 | 내용 |
|---|---|
| `src/data/outcomes.yaml` | 논문·특허·SW·수상 **42건**, type 5종 |
| `/{locale}/publications/` | 유형별 목록 + 상단 집계. 네비게이션에 '논문·특허' 추가 |
| 랜딩 KPI | **전부 자동 산출로 전환** — 결과물 39 · 논문 25 · 특허 14 (`AUTO_*` 표식) |

KPI를 손으로 세지 않는 규칙을 이번에 논문·특허까지 확대했다. 38/39 오기가 재발할 여지를 없앤다.

### 기술 활용 사례 게재 (2026-08-05, D-22)

사용자 지시로 **사례명만** 싣는다. 매출액·계약 상대(사업화기관명)는 담지 않는다.

**중복 발견:** 성과보고서 원표는 23행이나 익산 미륵사지 석등(7·8행)과 대구 온라인 컨퍼런스 플랫폼(17·18행)이 **각각 두 번 기재**되어 있다. 중복을 그대로 세면 "23건"이 되어 틀린다. **고유 사례는 21건**(2020년 3 · 2021년 11 · 2022년 7).

- `src/data/applications.yaml` 21건, ko/en 병기
- 랜딩 04 섹션 신설 → 참여기관 05, 연락처 06으로 재번호
- **유출 검사 실측:** 빌드 산출물 전체에서 금액 패턴 0건, 보고서상 계약 상대(포스트미디어·엔디에스·한국자산관리공사·서울역사박물관) 0건. "화천박물관"은 사례명 자체에 포함된 것이므로 의도한 범위

### ⚠️ 사고 기록 — 대외비 이미지 공개 (2026-08-05)

**무슨 일:** `robot-arm.webp`로 저장한 이미지가 로봇팔 사진이 아니라 **사업화 실적표 스크린샷**이었다. 표 안에 싣지 않기로 한 계약 상대 기관명(한국자산관리공사·서울역사박물관·포스트미디어·엔디에스 등)이 그대로 들어 있었고, 커밋 `d8e29e2`로 **공개 저장소에 push되었다.**

**원인:** 268건 이미지를 컨택트시트로 선별할 때 **라벨을 셀 하단에 그려** 다음 행 이미지의 라벨로 오독했다. 한 행이 밀린 상태로 12건을 매핑했고, 그중 4건이 잘못 배치되었다(사업화 표·Paired/Unpaired·색상 팔레트·지도 시각화).

**조치:**
1. `70e618a` — 파일과 참조 삭제, push
2. 컨택트시트를 **라벨을 이미지 위에 겹쳐 그리는 방식**으로 재생성 (`idx-*.jpg`)
3. 39건 전량을 재선별하고 **변환 전에 검증 몽타주로 육안 확인**
4. 빌드 산출물에서 계약 상대 기관명·금액 문자열 0건 재확인

**히스토리 재작성 실행 (2026-08-05, 사용자 승인):**

1. 전체 백업 번들 생성 (`chic-backup.bundle`, 4.2MB) — 저장소 밖 보관
2. 유출 blob을 **ID로 특정** (`2c0355ec…`, 141,968 bytes). 같은 경로의 현재 로봇팔 이미지(`9c703b42…`, 11KB)는 보존해야 하므로 경로 삭제가 아니라 blob 삭제를 택함
3. `git filter-repo --strip-blobs-with-ids` 실행 → 10커밋 유지, `d8e29e2` 이후 SHA 변경
4. 검증: 유출 blob 제거·현재 이미지 보존·빌드 통과·추적파일 107건 이상 없음
5. `git push --force` 완료 (`5370e02` → `0690a8e`)

**⚠️ force push만으로는 부족했다 (실측):** 옛 커밋 `d8e29e2`가 원격에서 SHA로 여전히 접근되고, 유출 파일이 **동일 해시·동일 크기로 그대로 다운로드된다.** GitHub는 도달 불가 객체를 자체 GC 전까지 보관한다. 사용자 판단으로 **저장소 삭제 후 재생성**을 택했으나, `gh` 토큰에 `delete_repo` 스코프가 없고 이 스코프는 브라우저 인증이 필요해 **비대화형 세션에서 수행 불가**. 사용자 직접 조치 대기.

**최종 해결 (2026-08-05):** 사용자가 옛 저장소 `chic-site`를 **삭제**하고 새 저장소 `ETRI-ULSOO/CHIC-Homepage`를 생성했다. 정리된 히스토리(11커밋·107파일)를 새 저장소에 push 완료.

**유출 제거 실증:** 옛 저장소의 유출 커밋 `d8e29e2` 조회 → HTTP 409 `Git Repository is empty`, 유출 파일 조회 → HTTP 404. **저장소 삭제는 도달 불가 객체까지 함께 제거하므로 force push와 달리 확실하다.** 새 저장소의 `robot-arm.webp`는 올바른 11,284 bytes(로봇팔 장치)임을 해시로 확인.

**남은 정리:** 빈 껍데기로 재생성된 옛 저장소 `ETRI-ULSOO/chic-site`가 public으로 남아 있다 — 혼선 방지를 위해 삭제 권장 (M-19).

**구 미해결 기술:** 삭제해도 `d8e29e2` 커밋에 파일이 남아 있어 **git 히스토리에서는 여전히 접근 가능하다.** 완전 제거하려면 히스토리 재작성(`git filter-repo` 등) 후 force push가 필요하며, 공개 저장소이므로 이미 클론·캐시된 사본까지는 회수할 수 없다. 사용자 판단 필요.

**교훈:** 자동 선별한 이미지를 그대로 쓰지 않는다. **변환 전에 대상과 라벨을 함께 눈으로 확인하는 단계**를 반드시 거친다. 특히 비공개 원본에서 뽑은 자산은 내용까지 확인해야 한다.

### Results 이미지 39건 전량 반입 (2026-08-05)

재선별한 39건을 전량 연결했다(합계 2.5MB). 이미지 있는 카드와 없는 카드가 섞여 그리드 높이가 들쭉날쭉하던 문제도 함께 해소됐다.

| 분류 | 대표 이미지 |
|---|---|
| 획득·품질 개선 10 | RGB 팔레트 색상 보정 · L1-2 디블러링 · Paired/Unpaired 비교 · 로봇팔 장치 · 해상도 평가 차트(800/1200mm) |
| 애셋 기술 8 | 애셋 관리 UI · 3D 스캔 컬러맵 · 가상 전시 · PBR 재질 30종 · 3D 저작도구 |
| 지능형 분석·검색 9 | 객체 목록 데이터셋 · 회화 객체 검출 · 관계 네트워크 · BERT/RoBERTa · 어노테이션 도구 |
| 큐레이션 플랫폼 8 | 전체 시스템 구성도 · 유물 검색 UI · 관계 시각화 · 웹뷰 |
| 전시·체험 4 | ENGAGE VR · 웹 박물관 3D · 관람객 동선 배치도 |

### Cloudflare 배포 실패와 해결 (2026-08-05)

**증상:** 첫 배포에서 `astro build`는 20페이지로 **성공**했으나(`Success: Build command completed`), 이어지는 `npx wrangler deploy`에서 실패.

```
Error: No such module "node:fs".
  imported from "dist/server/.prerender/chunks/content.config_*.mjs"
```

**원인 (로그 실측):** 프로젝트가 Pages가 아니라 **Worker로 생성**되어 배포 명령이 `npx wrangler deploy`였다. `wrangler.jsonc`가 없으니 wrangler가 자동 구성을 돌려 **`astro add cloudflare`로 어댑터를 설치**했고, 정적 사이트가 서버 렌더링 모드로 **재빌드**되었다. `content.config.ts`는 빌드 시점에 `node:fs`로 YAML·Markdown을 읽는데 Workers 런타임에는 `node:fs`가 없다.

즉 **빌드가 두 번 돌았고, 두 번째(어댑터가 붙은) 빌드가 터진 것**이다. 첫 빌드 로그만 보면 성공으로 보여 오진하기 쉽다.

**해결:** 되돌리는 대신 Workers 정적 자산 배포로 제대로 설정했다.
- `wrangler.jsonc` 신설 — `assets.directory: ./dist`, **`main` 없음**(실행할 서버 코드가 없다). 설정이 있으면 자동 구성이 건너뛴다
- `src/pages/404.astro` 신설 — `not_found_handling: "404-page"`가 `/404.html`을 요구한다
- `wrangler`를 devDependency로 고정
- `.gitignore`에 `.wrangler/` 추가

**검증 (실측):** `npx wrangler deploy --dry-run` → `Read 242 files from the assets directory`, 자동 구성 미실행, 바인딩 0. 빌드 21페이지(404 포함).

**교훈:** 빌드 성공 로그를 보고 판단하지 않는다. Cloudflare는 build 단계와 deploy 단계가 분리되어 있고 **deploy 단계에서 프로젝트를 재구성할 수 있다.**

### 기관 도메인(chic.etri.re.kr) 가능성 조사 (2026-08-05)

**배경:** 배포 주소가 `chic-homepage.hkkim79.workers.dev`로 **개인 Gmail 계정 서브도메인이 노출**된다. 종료 과제 아카이브 주소로 부적절하고 계정 종속 위험이 있어 기관 도메인 연결 가능성을 조사했다.

**방법:** 3개 주제(Workers / Pages / GitHub Pages)를 병렬 조사 후 각각 적대적 검증. **3건 모두 반증 실패, 신뢰도 high** (2026-08-05, 공식 문서 원문 직접 대조).

**결정적 사실:**

> "Unlike Pages, **Workers does not support any domain whose nameservers are not managed by Cloudflare**."
> — developers.cloudflare.com/workers/static-assets/migrate-from-pages

`etri.re.kr` 네임서버는 ETRI에 있고 Cloudflare로 이전 불가하므로, **현재의 Workers 배포로는 기관 도메인을 붙일 수 없다.** 호환성 매트릭스에도 "Custom domains outside Cloudflare zones" = Workers ❌ / Pages ✅ 로 명시.

**역설:** D-03에서 "기관 도메인 정책이 열리면 CNAME으로 이관"을 염두에 두고 Cloudflare를 골랐는데, Workers로 배포되면서 바로 그 경로가 막혔다.

| 경로 | 기관 도메인 | 비용 | 개인 계정 종속 | 비고 |
|---|---|---|---|---|
| A. Cloudflare Pages 전환 | ✅ 서브도메인 CNAME | 0원 | 남음 | `_headers`·빌드 그대로 유지 |
| B. GitHub Pages (ETRI-ULSOO) | ✅ CNAME → `ETRI-ULSOO.github.io` | 0원 | **없음** | 형제 사이트와 동일 방식, `_headers` 사용 불가 |
| C. Cloudflare for SaaS | ✅ | 0원(100건 내) | 남음 | 별도 소유 도메인 필요, 구성 복잡 |
| D. 존 이전 (full setup) | ✅ | 0원 | 남음 | **기관 네임서버 변경 — 사실상 불가** |
| E. CNAME setup (partial zone) | ✅ | **$200+/월** | 남음 | Business 플랜 전용, 비용상 배제 |

**공통 전제:** ETRI 정보화 부서가 `chic.etri.re.kr` CNAME 레코드를 생성해야 한다. **외부 호스팅으로의 CNAME 허용 여부가 첫 관문.**

**공통 주의(양쪽 공식 문서 경고):** 반드시 **호스팅 쪽에 도메인을 먼저 등록한 뒤 DNS 레코드를 만든다.** 역순은 서브도메인 탈취 위험. Pages는 역순 시 522 오류.

**GitHub Pages 세부 (확인됨):** CNAME 대상은 `<organization>.github.io`이며 **저장소명을 포함하지 않는다.** HTTPS는 Let's Encrypt로 자동 발급(최대 1시간), Enforce HTTPS 옵션은 최대 24시간. 도메인 검증(TXT)은 권장이나 필수 아님. 커스텀 Actions 워크플로 게시 시 CNAME 파일이 무시되므로 저장소 설정으로만 관리.

**Cloudflare Pages 세부 (확인됨):** 2026년 현재 신규 프로젝트 생성 **여전히 가능**. 다만 공식 권고는 "신규 프로젝트는 Workers 권장". 검색 상단의 "Pages deprecated" 서술은 전부 제3자 블로그이며 공식 폐기 공지는 없음.

**현재 상태:** 사용자 판단 보류. 배포는 Workers(`chic-homepage.hkkim79.workers.dev`) 유지 (M-21).

### Cloudflare Pages로 전환 (2026-08-05, D-23)

**결정 근거 두 가지:**
1. **기관 도메인.** Workers는 네임서버가 Cloudflare 관리 하에 있지 않은 도메인을 지원하지 않는다(공식 문서). `chic.etri.re.kr`을 붙이려면 Pages가 유일한 무료 경로다.
2. **주소에서 개인 계정명 제거.** Workers는 `<worker>.<계정서브도메인>.workers.dev`라 `hkkim79`가 노출됐다. Pages는 `<프로젝트명>.pages.dev`로 계정명이 들어가지 않는다.

계정 서브도메인만 바꾸는 방법(`hkkim79` → `etri-chic`)도 확인했으나, 그 경우 나중에 기관 도메인을 붙일 때 결국 Pages로 옮겨야 하고 **주소가 두 번 바뀐다.** 색인·인용 주소의 흔들림을 한 번으로 줄이기 위해 지금 전환한다.

**변경 내역**

| 항목 | 처리 |
|---|---|
| `wrangler.jsonc` | 삭제 (Workers 전용) |
| `wrangler` devDependency | 제거 |
| `.gitignore`의 `.wrangler/` | 제거 |
| `astro.config.mjs` `site` | `https://etri-chic.pages.dev` |
| [[README]] 배포 절 | Pages 기준으로 재작성 + 커스텀 도메인 절차 |

**유지되는 것:** 빌드 설정, `.nvmrc`(22), `public/_headers`(Pages도 지원), `src/pages/404.astro`(Pages가 `dist/404.html`을 자동 사용), 콘텐츠·이미지 전부.

**남은 조치 (사용자):** ① Pages 프로젝트 `etri-chic` 생성 ② 기존 Worker `chic-homepage` 삭제(혼선 방지) ③ 이름이 선점돼 있으면 다른 이름 + `site` 값 동반 수정.

### 남은 미해결

| # | 항목 |
|---|---|
| M-01 | Consortium·News 영문 콘텐츠 신규 번역 필요 여부 |
| M-02 | 다이어그램 원본 보유분 확인 및 전달 (A-02) |
| M-03 | `/project` ↔ `/project/about` 중복 통합 여부 |
| M-04 | Ambition 페이지(이미지 1장) 독립 유지 여부 |
| M-06 | News 첨부 14건(Google Drive 링크) 저장소 이관 여부 — 링크 유지로 구현했으나 영속성 문제는 남는다 (P-05) |
| M-07 | Results 실제 항목 수 39건 확정 — 필터·연차 구분 UI 필요 여부 (A-03 대체) |
| ~~M-08~~ | ~~업무 계정 커밋 이메일~~ → **해소 (2026-08-03).** `hkkim79@etri.re.kr` 저장소 로컬 설정, 초기 2커밋 후 `ETRI-ULSOO/chic-site`(**public**)로 푸시 완료 |
| M-12 | **저장소가 공개 상태다.** M-11(재구성 다이어그램 3점 검수)이 미해결인 채로 공개되었으므로 검수 우선순위가 올라간다 |
| ~~M-09~~ | ~~폰트 자체호스팅~~ → **해소 (2026-08-03).** 외부 폰트 요청 0건 실측 |
| ~~M-10~~ | ~~영문판 다이어그램~~ → **해소 (2026-08-03).** `*-en.svg` 7점 제작·연결 |
| M-11 | `concept-strategy.svg` 등 재해석 다이어그램 3점의 내용 검수 |
| M-13 | **Results 1차년도 15건의 대분류 역매핑 검수.** 현행 사이트에 1차년도 분류가 없어 내용 기준으로 5종에 배정했다 |
| M-14 | Results 결과물 이미지 — 39건 중 **12건 반입 완료**(2026-08-05). 나머지 27건 미확보 |
| ~~M-16~~ | ~~사업화 공개 여부~~ → **결정 (2026-08-05, D-22): 사례명만 공개, 금액·계약 상대 비공개.** 랜딩 04 '기술 활용 사례' 섹션에 21건 게재. 빌드 산출물에서 금액 0건·별도 계약상대 0건 실측 확인 |
| M-16-b | 원 판단 근거 (보관) — 성과보고서에 사업화 23건·매출 59.2억(2020 9.2억/2021 18.9억/2022 31.0억)·직접고용 13명이 있다. 사례명(미륵사지 석등 디지털복원, 조선왕릉 실감콘텐츠, 클리블랜드미술관, 광화벽화 등)은 공개형 사업이나 **계약 상대·금액은 대외비 성격**이라 사용자 판단 전까지 사이트에 싣지 않았다 |
| ~~M-18~~ | ~~히스토리 유출~~ → **해소 (2026-08-05).** 저장소 삭제·재생성으로 완전 제거, 실증 확인 |
| M-19 | 빈 저장소 `ETRI-ULSOO/chic-site` 삭제 (혼선 방지) — 2026-08-05 확인 시 여전히 존재, 내용은 비어 있어 위험 없음 |
| ~~M-21~~ | ~~기관 도메인 경로~~ → **결정 (2026-08-05, D-23): Cloudflare Pages 전환.** 저장소 측 준비 완료, 대시보드 프로젝트 생성 대기 |
| M-22 | Pages 프로젝트 `etri-chic` 생성 후 기존 Worker `chic-homepage` 삭제 (중복 배포 방지) |
| M-21-b | 구 기술 — Workers로는 불가하므로 Pages 또는 GitHub Pages 전환이 선행되어야 한다. 조사 결과는 위 절 참조 |
| ~~M-20~~ | ~~Cloudflare 빌드 설정~~ → **해소 (2026-08-05).** `wrangler.jsonc`로 Workers 정적 자산 배포 구성, dry-run 검증 완료 |
| M-20-b | 구 기술 — `NODE_VERSION=22` 필수 (Astro 7 engines `>=22.12.0`, 기본값으로는 빌드 실패). 프로젝트명을 `chic-homepage`로 생성 — `astro.config.mjs`의 `site` 값과 일치시켜야 함 |
| M-17 | **반입 이미지의 저작권·공개 범위 확인.** 최종보고서 수록 이미지이며 상당수가 국립중앙박물관 소장품 촬영·3D 스캔 결과다. 과제 자체 홍보 사이트 사용은 통상적이나 확인이 필요하다 |
| M-15 | **Cloudflare Pages 대시보드 최초 연결** — 계정 접근이 필요해 미수행. 설정값은 [[README]]에 정리 |

### 세션 종료 시점 (2026-08-03)

**완료:** G1~G4.5 전 게이트, G5의 페이지 구현 전체.
사이트는 한/영 양쪽 전 페이지가 동작하며 `npm run build` 통과 상태다.
저장소 `ETRI-ULSOO/chic-site`(public)에 push 완료, 작업 트리 깨끗함.

| 구성 | 상태 |
|---|---|
| 랜딩 (과제소개·기대효과·필요성·참여기관·연락처) | ✅ |
| Concept · Results(39건 필터) · News(5건 목록+본문) | ✅ |
| 다이어그램 국문 7 + 영문 7 | ✅ |
| 구 URL 리다이렉트 15 | ✅ |
| 폰트 자체호스팅 (외부 요청 0) | ✅ |
| **Cloudflare Pages 연결** | ❌ **M-15 — 계정 접근 필요** |

### 다음 세션 착수 순서

1. **검수 3건 처리** — M-11(재구성 다이어그램) · M-13(1차년도 분류) · D-17(영문 용어).
   저장소가 공개 상태이고 배포가 임박했으므로 이것이 최우선이다.
2. **Cloudflare Pages 연결** (M-15) — 대시보드에서 저장소 연결. 설정값은 [[README]] 참조.
   프로젝트명을 `chic-site` 외로 만들 경우 `astro.config.mjs`의 `site` 값을 맞춰야 한다.
3. **`DECISIONS.md` 신설** — 현재 결정 D-07~D-20이 전부 이 파일에 섞여 있다.
   프로토콜 §7은 결정을 별도 파일로 두도록 한다. 진행 로그와 결정 로그의 분리가 필요하다.
4. Results 결과물 이미지 39건 확보 (M-14), News 첨부 영속성 검토 (M-06)

### 이 세션에서 내가 틀렸던 것 (재발 방지용)

1. **"1:1 이관"을 정보구조+디자인 모두로 해석** — 실제 의도는 구조 보존 + 디자인 리뉴얼이었다.
   사용자가 중단시킨 뒤에야 드러났다. 범위 해석이 갈릴 수 있는 지점은 착수 전에 확인할 것.
2. **재색조 불가 제약을 재색조 가능한 자산에 적용** — 다이어그램 규칙 오류. 제약의 근거가
   사라진 자산에까지 규칙을 기계적으로 적용하지 말 것.
3. **결과물 수를 손으로 세어 38로 오기** — 실제 39. 수치는 데이터에서 산출할 것.
4. **다이어그램 2점을 만든 뒤 배치 단계에서 중복 발견** — 만들기 전에 놓일 자리를 먼저 볼 것.

---

## 2026-08-05 (3) — GitHub Pages 전환

### 목표

Cloudflare Pages 대시보드에서 프로젝트 생성 진입점이 사라진 상황(사용자 실측)을 받아,
배포 대상을 **GitHub Pages**로 전환한다. 저장소가 이미 GitHub에 있으므로 외부 서비스
연결과 계정 노출이 모두 사라지는 경로다.

### 결정

**[D-24] 배포 대상을 GitHub Pages로 전환한다 (D-23 대체).**

- 근거 1 — 계정 접근 장벽 소거. Cloudflare Pages는 대시보드 조작이 필요했고 그 진입점이
  실제로 보이지 않았다. GitHub Pages는 저장소 설정 한 번이면 되고, 이후는 워크플로 파일이
  단일 원천이 된다.
- 근거 2 — 기관 도메인 요건 충족. `chic.etri.re.kr` CNAME → `etri-ulsoo.github.io`로
  서브도메인 연결이 가능하며 Let's Encrypt 인증서가 자동 발급된다. Workers를 기각했던
  사유(외부 네임서버 도메인 연결 불가)에 걸리지 않는다.
- 근거 3 — 주소에 개인 계정명이 없다. `etri-ulsoo.github.io`는 기관 조직 계정이다.
- 대가 — **응답 헤더를 설정할 수 없다** (M-23) 와 **project site라 `/CHIC-Homepage/`
  하위 경로가 된다.** 후자가 코드에 실질적 영향을 준 부분이다.

**[D-25] base 처리를 `withBase()`/`stripBase()` 두 함수로 일원화한다.**

Astro는 번들 자산에는 base를 자동으로 붙이지만 **소스에 하드코딩한 절대경로에는 붙이지
않는다.** 각 호출부에서 문자열을 조합하면 누락 지점이 반드시 생기므로 [[url.ts]]에
두 함수를 두고 전 호출부를 통과시켰다. 커스텀 도메인 연결 시 `BASE`만 비우면 전부
무해해지는 구조다.

### 산출물

| 항목 | 내용 |
|---|---|
| `src/lib/url.ts` (신규) | `withBase()` — 내부 절대경로에 base 부착 (외부 URL·mailto·앵커는 통과) / `stripBase()` — 경로 해석 전 base 제거 |
| `src/i18n/utils.ts` | `getLocale`이 `stripBase` 후 판별. **이것이 없으면 첫 세그먼트가 `CHIC-Homepage`로 읽혀 전 페이지가 기본 언어로 떨어진다** |
| `astro.config.mjs` | `site: https://etri-ulsoo.github.io` · `base: '/CHIC-Homepage'` · **리다이렉트 대상 15건에 base 수동 부착** |
| `.github/workflows/deploy.yml` (신규) | `main` push → 빌드 → `deploy-pages`. OIDC 인증, 외부 토큰 없음 |
| `public/.nojekyll` (신규) | 없으면 Jekyll이 `_astro/`를 무시해 CSS·폰트 전량 404 |
| `public/_headers` (삭제) | GitHub Pages 미지원 |
| 링크·자산 수정 | Header 7 · index 2 · concept 2 · results 1 · news 2 · 404 2 |

### 실측 검증 (2026-08-05)

| 검사 | 결과 |
|---|---|
| `npm run build` | 21페이지 통과 |
| 빌드 산출물 내부 참조 334건의 base 부착·대상 존재 | **누락 0 · 끊김 0** |
| 리다이렉트 15건 대상 | 전건 `/CHIC-Homepage/…`로 교정 |
| canonical ↔ hreflang 쌍 | 대칭 일치 |
| dev 서버 실 요청 (`/ko/`, `/ko/results/`) | 404 0건, Results 이미지 39/39 로드 |
| 외부 리소스 요청 | 0건 (본문 `<a>` 외부 링크는 의도된 것) |
| 비공개데이터 추적 | 0건 |

### 교정한 결함 2건

1. **리다이렉트 대상에 base 미부착.** Astro는 redirects의 **출발 경로에만** base를 붙이고
   대상에는 붙이지 않는다 (실측). 15건 전부가 `/ko/…`로 나가 404가 될 상태였다.
   config에서 대상을 직접 합쳐 교정.
2. **hreflang 끝 슬래시 불일치.** `alternatePath`가 2세그먼트 이상에서 끝 슬래시를 빼
   `…/ko/results`를 내보냈다 — 상대 페이지의 canonical(`…/ko/results/`)과 다른 URL이라
   언어 쌍이 성립하지 않는다. 항상 붙이도록 수정.

### 남은 미해결 (추가)

| # | 항목 |
|---|---|
| M-23 | **응답 헤더 부재.** `Referrer-Policy`만 meta로 대체했다. `X-Frame-Options`·`X-Content-Type-Options`는 meta 등가물이 없고 CSP `frame-ancestors`는 명세상 meta에서 무시된다. 공개 정적 사이트라 실질 위험은 낮음 |
| ~~M-24~~ | ~~Pages Source 지정~~ → **해소 (2026-08-05).** `gh api -X POST repos/ETRI-ULSOO/CHIC-Homepage/pages -f build_type=workflow`로 활성화. `https_enforced: true` |
| M-22 | (갱신) Cloudflare Worker `chic-homepage` 삭제 — 이제 Pages 프로젝트 생성이 아니라 **Cloudflare 자원 정리** 항목이다 |
| M-15 | (해소) ~~Cloudflare Pages 대시보드 연결~~ → D-24로 무효화 |

### 배포 완료 — 라이브 실측 (2026-08-05)

**https://etri-ulsoo.github.io/CHIC-Homepage/** 에서 서비스 중.
워크플로 첫 실행은 Pages 활성화 **이전**에 시작되어 `configure-pages`가 *Get Pages site
failed*로 실패했다 — 활성화 후 재실행하여 build 24초 · deploy 8초로 성공.

| 검사 항목 | 결과 |
|---|---|
| 페이지 13개 (한/영 전 경로 + News 본문) | 전건 200 |
| 구 URL 리다이렉트 6종 표본 | 전건 200, 정상 착지 |
| 자산 (SVG 국·영문, 사진, Results 이미지) | 전건 200 |
| Results 이미지 | **39/39 로드, 깨짐 0** |
| 없는 경로 | 404 + **커스텀 404 페이지** 응답 확인 |
| 스타일·폰트 적용 | `body` 배경 `rgb(11,14,17)`, `h1` = Noto Serif KR (자체호스팅 동작) |
| HTTPS | `https_enforced: true` |

`.nojekyll`이 실제로 동작함을 여기서 확인한 셈이다 — 없었다면 `_astro/`의 CSS가
404가 되어 스타일 검사가 실패했을 것이다.

### 다음 단계

1. 검수 3건 (M-11 · M-13 · D-17) — 사이트가 공개 배포된 상태이므로 최우선
2. M-17 이미지 저작권·공개 범위 확인
3. 커스텀 도메인 `chic.etri.re.kr` 요청 ([[README]] 절차 순서 준수)
4. Cloudflare 자원 정리 (M-22), 빈 저장소 삭제 (M-19)

---

## 2026-08-05 (4) — 판독성 개선: 타입 스케일 + 다이어그램

### 목표

사용자 피드백 2건에 대응한다. ① "전체적인 글씨가 너무 작다" ② "SVG로 만든 녀석들은
핸드폰으로 보면 너무 작게 보인다".

### 진단 (전부 브라우저 실측, 2026-08-05)

**① 글씨 — 값이 아니라 배정이 무너져 있었다.**

토큰상 본문은 15px인데 랜딩에서 15px로 조판된 텍스트는 **5곳뿐**이었다. 실제 텍스트의
대부분이 그 아래 두 칸에 몰려 있었다 — 11px 52회, 13px 61회. 논문·특허 페이지는 더 심해
**11px가 126회**로 페이지의 지배적 크기였다.

원인은 세 겹이다.
1. `ul { font-size: var(--t-body-sm) }` — 연구목표·연구내용이 본문인데 보조 텍스트 크기로 조판.
   주 내비게이션도 13px.
2. `.micro`의 `letter-spacing: 0.12em` + `text-transform: uppercase` — 둘 다 **라틴 소문자용
   장치**다. 한글에 대문자는 없어 변환이 무효이고, 자간만 남아 어절을 낱글자로 흩어 놓는다.
3. `h3 { font-size: var(--t-subheading) }` (publications) — **미정의 토큰.** 논문·특허 제목
   42건이 22px 헤딩이 아니라 15px 본문 크기로 렌더되고 있었다. 이미 배포된 상태였다.

부가 요인: 1920px 모니터에서 콘텐츠가 화면의 59%만 쓰고 좌우 390px씩 비어 작다는 체감을 키운다.

**② 다이어그램 — 도해가 작은 게 아니라 축소되고 있었다.**

전 14점이 viewBox 폭 1280 고정에 지배 라벨 10.5~11px다. 컨테이너에 맞춰 축소하면 라벨이
같은 배율로 함께 줄어든다.

| 뷰포트 | 렌더폭 | 배율 | 10.5px 라벨 실제 |
|---|---|---|---|
| 1440 데스크톱 | 1092px | 0.85 | **8.96px** |
| 768 태블릿 | 720px | 0.56 | 5.91px |
| 375 모바일 | 325px | 0.25 | **2.67px** |
| 375 **라이트박스** | 360px | 0.28 | **2.95px** |

**라이트박스가 모바일에서 1.11배(325→360px)만 키운다** — 96vw가 인라인 폭과 거의 같아
확대 기능이 사실상 무동작이었다. 컴포넌트 주석의 "확대는 기능 요건" 전제가 깨져 있었다.

폭별 육안 판정으로 판독 하한을 잡았다: 560px 불가 · 800px 겨우 · **1092px(현 데스크톱)이 한계선**.

### 결정

**[D-26] 축소를 금지한다 — 폭이 모자라면 줄이는 대신 가로 스크롤한다.**

`min-width: <원본 폭>`을 프레임에 걸어 원본 아래로는 절대 축소하지 않는다. 1200px 이상에서는
반대로 도해만 본문 컬럼 밖으로 빼 원본보다 크게 띄운다(최대 1440px). 라이트박스는
`max(96vw, 1600px)`로 **항상 인라인보다 크게** 만든다.

기각한 대안 — **SVG 14점 내부 미디어쿼리 반응형(좁은 폭 전용 레이아웃)**. `<img>`로 참조된
SVG의 내부 미디어쿼리가 페이지 뷰포트가 아니라 **SVG 렌더 크기**에 반응한다는 것은 직접
실증했다(320px 렌더에서 발동, 900px에서 미발동). 그러나 내부 `font-size`는 viewBox 좌표
값이라 글자만 키우면 박스 레이아웃이 깨진다 — 좁은 폭 전용 조판을 따로 그려야 하고, 국·영
× 넓은·좁은으로 문안이 4중화되어 "심사위원이 폰으로 본 그림과 데스크톱으로 본 그림의 문구가
다르다"는 부채가 영구화된다. 게다가 D-26을 적용하면 렌더 폭이 항상 1280px 이상이라 좁은
판이 **발동조차 하지 않는다** — 두 안은 논리적으로 양립 불가다.

**[D-27] 언어 판별은 `:lang(ko)`로 한다 — 클래스를 나누지 않는다.**

`<html lang>`과 LangSwitch의 `lang` 속성이 이미 언어를 정확히 알려준다. 한글 전용 클래스를
신설하면 26곳의 마크업을 옮겨야 하고 그중 하나만 빠져도 규칙이 깨진다. `:lang(ko)` 한 규칙이면
국문 페이지의 모든 `.micro`가 한글 처리를 받고, `lang="en"`을 단 'English' 링크만 라틴 처리를
유지한다. 데이터가 페이지 언어와 어긋나는 경우(영문 페이지의 한글 note)는 빌드 시점에
문자열을 보고 `lang` 속성을 붙인다.

### 산출물

| 파일 | 변경 |
|---|---|
| `tokens.css` | 8개 타입 토큰 상향, `--t-kpi`·`--tracking-micro` 신설, `--lh-body` 1.8→1.75, `:lang(ko) .micro` 규칙 |
| `index.astro` | 산문 리스트만 `--t-body`+`--c-text`로 승격(태그 선택자가 사례·기관·연락처 목록까지 잡으므로 명시 분리), lead 행간 1.9→1.7 |
| `DiagramFigure.astro` | 스크롤 컨테이너 + `min-width` 판독 하한 + 데스크톱 브레이크아웃 + 라이트박스 재설계 + 조건부 tabindex + 드래그/클릭 분리 |
| `KpiStat` · `publications` | `.v` 26px 하드코딩 2곳 → `--t-kpi`, `.k`를 `.micro`에서 `--t-body-sm`으로 |
| `publications.astro` | **미정의 `--t-subheading` → `--t-heading-sm`** (논문 제목 42건 정상화) |
| `Header` · `LangSwitch` | 워드마크 20px→`--t-heading-sm`, 자간 토큰화 |
| 그리드 8곳 | `minmax(300px,1fr)` → `minmax(min(300px,100%),1fr)` — 320px 오버플로 해소 |
| `DESIGN.md` | Type Scale 표·배정 규칙·라틴 전용 규칙 신설, 12px 하한 개정, DiagramFigure 절 재작성, Quick Start 값 동기화 |

`--measure`는 **68ch를 유지**했다. 심사안은 58ch를 제시했으나 실측상 68ch가 이미 한 줄
45.9자로 한글 최적 구간(40~50자)에 있고, 58ch는 39.2자로 하한을 밑돈다.

### 실측 검증 (2026-08-05, 9개 페이지 × 6개 뷰포트 = 54조합)

| 검사 | 결과 |
|---|---|
| 가로 오버플로 (320·375·768·1024·1440·1920) | **0건** |
| 12px 미만 글자 | **0건** |
| 한글에 대문자·자간 오적용 | **0건** |
| 내비 크기 / 타깃 높이 | 15px / 26.3px (WCAG 2.5.8 통과, 패딩 불요) |
| 다이어그램 라벨 실효 크기 | **10.50~11.80px** (종전 2.67~8.96px) |
| 라이트박스 회귀 | 모바일 1600px(13.13px) · 데스크톱 1600px, 항상 인라인보다 큼 |
| 크기 분포 (랜딩) | 11px·13px **0회**, 12px 53 · 15px 55 · 17px 15 |
| 빌드 산출물 내부 참조 334건 | base 누락 0 · 끊김 0 |
| DESIGN.md ↔ tokens.css 토큰 | **전 항목 일치** |

### 교정한 결함 3건 (실측이 아니었으면 놓쳤을 것)

1. **`ResizeObserver` 인스턴스 미참조.** 변수에 담지 않아 수거되어, 데스크톱→모바일 리사이즈 시
   `tabindex`·안내문이 갱신되지 않았다. 초기 로드만 보면 정상으로 보인다.
2. **LangSwitch 중괄호 위치 오류.** 편집 중 `border`·`padding`이 `:lang(ko)` 블록으로 들어가
   국문 페이지에서 언어 전환 알약의 테두리가 사라졌다. 수치 검사는 전부 통과했고 **스크린샷이
   잡았다.**
3. **320px 그리드 오버플로.** `minmax(300px,1fr)`가 257px 컨테이너에서 넘쳤다. 글자 크기와
   무관한 기존 결함이며 WCAG 1.4.10 위반이었다.

### 미채택 — 검증 불가능한 것은 넣지 않았다

스크롤 어포던스용 그라디언트 페이드를 만들었으나, 이 환경에서 규칙이 적용되지 않고(선택자는
매칭되는데 computed opacity가 0으로 남음) 스크롤 이벤트도 발생하지 않아 상태 전이를 끝까지
확인할 수 없었다. **검증하지 못한 장식 코드를 남기는 대신 제거했다.** 어포던스는 실측으로
확인된 두 가지 — 네이티브 스크롤바와 캡션 안내문("좌우로 밀어 보세요") — 로 유지한다.

### 남은 미해결 (추가)

| # | 항목 |
|---|---|
| M-25 | **영문 페이지에 미번역 한글 note 4건** — outcomes.yaml의 `note`가 언어별로 갈리지 않아 `/en/publications/`에 "비SCI", "SCIE · 성과보고서 제출 이후 게재"가 그대로 노출된다. 조판은 `lang` 속성으로 교정했으나 번역은 내용 결정이 필요하다 |
| M-26 | SVG 원본 활자 상향(2단계) — 14점에 정적 폰트 스케일을 넣으면 모바일 12.5px·데스크톱 13.6px까지 오르고 패닝 거리도 준다. 라벨별 박스 넘침 전수 검사가 선행 조건. 실제 font-size는 6종이 아니라 **8종**(10.5·11·12·12.5·13·13.5·14·20)이므로 규칙을 그 전부에 걸어야 한다 |
| M-27 | `<img>` 참조 SVG의 내부 텍스트는 보조기술에 노출되지 않고 브라우저 최소 글꼴 설정에도 반응하지 않는다(WCAG 1.4.5 images of text). 이번 방향으로는 원리적으로 해결되지 않는다 — `alt`를 구조 서술로 확장하거나 본문에 동등 정보를 병기하는 저비용 완화부터 검토 |
| M-28 | `--t-display-en`은 사용처 0건인 죽은 토큰이다 (2026-08-05 실측) |

---

## 2026-08-05 (5) — 랜딩 도해 3점을 HTML/CSS로 재구축

### 목표

직전 작업(가로 스크롤)에 대한 사용자 질문 — "이게 최선인가, 핸드폰용으로 다른 형식을 주면
안 되나" — 에 답하고, 합의된 범위(랜딩 3점)를 구현한다.

### 진단 — 가로 스크롤이 못 고친 것

라벨 크기는 2.67px → 10.50px로 살렸지만 **조망을 잃었다.** 375px에서 한 번에 보이는 범위가
327/1280 = **25.5%**로, 전체를 보려면 4화면을 밀어야 한다. 도해의 가치는 "관계를 한눈에"인데
가로 스크롤은 글자만 살리고 그림을 죽인다. 게다가 중첩 컨테이너 안의 가로 스크롤은 폰에서
낯선 제스처라 안내문이 있어도 지나칠 수 있다.

**그와 별개로 더 큰 문제가 이미 있었다 (라이브 실측).** `<img>`로 참조된 SVG의 내부 문구
**341개가 페이지에서 완전히 보이지 않았다** — `innerText`에 없고, `svg text` 노드 접근 0개,
스크린리더는 `alt` 한 줄만 읽는다. 즉 Ctrl+F로 못 찾고 검색엔진이 못 읽는다. 과제의 핵심
성과 문구가 전부 그림 안에 갇혀 있었다.

### 결정

**[D-28] 랜딩 도해 3점을 HTML/CSS로 재구축한다. 모바일 전용 SVG 판본은 만들지 않는다.**

갈림길은 "다른 형식"이 **다른 파일**이냐 **같은 데이터의 다른 렌더링**이냐다.

- 모바일 전용 SVG(`-m.svg`) → 문안이 4벌(국/영 × 넓은/좁은). 심사위원이 폰과 PC에서 다른
  문구를 보게 되고, 수정할 때마다 4곳을 고쳐야 하며, 위 접근성 문제는 그대로다. **기각.**
- HTML/CSS → 문안 1벌, 반응형은 CSS가 처리, 접근성 문제가 통째로 해소. **채택.**

재구축이 가능한 근거는 실측이다: 이 도해들은 그림이 아니라 **박스 + 직선 화살표 격자**다
(project-overview: rect 8 · text 43 · 화살표 전부 직선 `M210 210 L248 210`, 곡선 0, 파일 4~6KB).

범위를 랜딩 3점으로 제한한 이유: 랜딩은 모든 방문자가 보지만 Concept 4점은 클릭해 들어가야
보인다. 노출 빈도 차이가 크고, 3점이면 결과를 보고 나머지를 결정할 수 있다.

**[D-29] 추진체계 도해는 문안을 갖지 않는다 — `consortium.yaml`에서 직접 생성한다.**

기관명은 이미 `consortium.yaml`에 `role`(lead/partner/collaborator)과 함께 있다. 도해용으로
다시 적으면 두 벌이 되어 언젠가 어긋난다. 종전 SVG는 같은 기관명을 국·영문 파일에 각각
박아 두어 목록까지 합쳐 **3벌**이 존재했다.

### 산출물

| 파일 | 내용 |
|---|---|
| `src/data/diagrams.yaml` (신규) | 랜딩 2점의 문안. 국·영문 한 벌. `stages`(화살표로 이어짐) · `band`(나열) · `footer` |
| `src/content.config.ts` | `diagrams` 컬렉션 + 스키마. 도해 한 줄은 `string \| { sub }` — `sub`는 위 줄의 부연 |
| `components/diagram/DiagramShell.astro` (신규) | 공통 껍데기 — 제목 바 · 캡션 · 푸터 |
| `components/diagram/DiagramStages.astro` (신규) | 단계 렌더러. 넓으면 가로 흐름, 좁으면 세로 + 화살표 회전 |
| `components/diagram/ConsortiumChart.astro` (신규) | `consortium.yaml`에서 생성하는 트리 |
| SVG 6점 삭제 | `project-overview{,-en}` · `necessity{,-en}` · `consortium-structure{,-en}` |
| `i18n/utils.ts` | `diagramSrc()` 제거 — 이번 변경으로 죽은 함수가 됐다 |
| `DESIGN.md` | 도해 절 재작성 (HTML 갈래 / SVG 갈래 구분) |

Concept 4점은 SVG를 유지하며 `DiagramFigure`(가로 스크롤·라이트박스)도 그대로 쓴다.

### 실측 검증 (2026-08-05, 2개 페이지 × 8개 뷰포트 = 16조합)

| 검사 | 결과 |
|---|---|
| 가로 오버플로 (320·375·600·768·900·1024·1440·1920) | **0건** |
| 12px 미만 글자 | **0건** |
| 도해 3점 렌더 | 전 조합 정상 |
| **도해 문구가 페이지 텍스트로 검색됨** | **국·영문 전 조합 true** ("기가픽셀", "관계 네트워크", "문화유산기술연구소" / "Gigapixel", "Relation networks", "Technology Research Institute") |
| 데스크톱 1440 배치 | 4단계 가로 흐름, 화살표 3개(22×1px), 4대 연구 축 2열 |
| 모바일 375 배치 | 세로 스택, 화살표 1×18px(↓), 도해 내부 가로 스크롤 0 |
| 빌드 산출물 내부 참조 322건 | base 누락 0 · 끊김 0 |

### 부수 효과

- **파일 14 → 8.** 랜딩 3점의 국·영문 SVG 6개가 사라졌다 (Concept 8개만 남음)
- SVG에서 폭 때문에 강제로 끊겼던 문장이 HTML에서 자연스럽게 이어졌다
  (예: "1. 문화유산 디지털 데이터 / 획득 및 품질 개선" → 한 문장)
- 도해가 본문 타이포 토큰을 상속한다 — 이번에 올린 스케일이 도해에도 적용된다

### 남은 미해결 (갱신)

| # | 항목 |
|---|---|
| ~~M-11~~ | ~~재구성 도해 3점 검수~~ → 랜딩 2점은 재구축 과정에서 문안을 전수 확인했다. **`concept-strategy.svg` 등 Concept 4점은 여전히 미검수** |
| M-27 | (범위 축소) `<img>` SVG의 접근성 문제 — 랜딩 3점은 해소, **Concept 4점에 남음** |
| M-26 | (조건부) SVG 원본 활자 상향 — 대상이 Concept 4점으로 줄었다. HTML 재구축을 Concept까지 확대하면 불필요해진다 |
| M-29 | Concept 4점의 HTML 재구축 여부 — 랜딩 결과를 보고 결정 |
